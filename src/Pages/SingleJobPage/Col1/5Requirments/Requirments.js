import styled from 'styled-components';



function Requirments({className=""}) {

  return (
    <DIV className={`${className}`}>
       
    </DIV>
  );
}


const DIV=styled.div`
    width: 100%;
    /* margin-top: var(--margin-top-fix-nav);  */  /*only Requirments */
    
 
`;

export default Requirments;
